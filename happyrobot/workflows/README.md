# happyrobot/workflows

Exports de los workflows de HappyRobot (JSON) cuando los haya, uno por fichero:
`onboarding-vecinos.json`, `rellamada.json`, `aviso-patrulla.json`. Hoy está vacía.

Los guiones que van dentro de cada nodo Agent/Generate/Extract están en `../prompts/`.
